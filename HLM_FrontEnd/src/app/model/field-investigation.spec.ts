import { FieldInvestigation } from './field-investigation';

describe('FieldInvestigation', () => {
  it('should create an instance', () => {
    expect(new FieldInvestigation()).toBeTruthy();
  });
});
